# 학습 날짜 : 2025년 02월 04일
# Introduction

## JAVA 언어의 특징


### 실행 방법

- 자바는 약간 다르게 컴파일러를 통해서 바이트 코드로 변경
- 이렇게 변경된 바이트 코드를 JVM이 읽어서 머신 코드로 변경해서 실행

.java (high level code) → java compiler (=javac) → .class (byte code) → JVM

### 객체 지향 언어

- 절차적 언어 : 기능으로 세분화 → 함수로 구현
    - 설계, 구현이 쉽지만 유지 / 보수, 확장이 어려움 ⇒ 객체 지향 프로그래밍 등장

### JVM, JRE, JDK

- JVM(JAVA Virtual Machine) : bytecode를 읽어서 실행하는 실행 주체
- JRE(JAVA Runtime Environment) : 자바 실행 환경
    
    ⇒ JVM + JAVA Class Library
    
- JDK (JAVA Development Kit): 자바 개발 도구
    
    ⇒ JRE + 자바개발도구 (javac, java, javadoc, etc..)
    

## JAVA의 객체 지향

### Class

1. 객체 모델링의 수단

현실세계의 구현해야 하는 문제를 프로그래밍적으로 표현(모델링)을 하는 구현 방법론

해결해야 하는 문제를 구성하고 있는 구성 요소를 파악한 후 프로그램으로 표현

- 추상화(Abstraction)를 거쳐서 나온 여러가지 정보를 상태와 행위로 구분
- 상태(State)는 변수(Fields)로, 행위는 함수(Method)로 표현

⇒ **Class** : 객체 모델링의 수단

따라서,  자바 프로그램은 Class의 집합

```java
// 클래스 이름은 첫글자 대문자로 작성
class Customer {
		// fields
	String name; // 고객 이름
	int age; // 고객 나이
	int height; // 고객 키
	
	// methods
	
}
```

이 클래스로부터 저장 공간을 생성해서 그 저장 공간을 이용해서 프로그래밍을 진행

⇒ **Instance** : Class로부터 생성된 저장 공간 (heap에 할당)

1. ADT (Abstract Data Type)

클래스는 데이터 타입의 역할 수행 

고정된 데이터 타입이 아니기 때문에 추상 데이터 타입

1. Instance를 파생시키는 수단

### Package

하나의 프로그램 내에는 많은 클래스가 존재하는데 이를 효율적으로 관리하기 위한 논리적인 개념

물리적으로 폴더로 구현

Class는 항상 Package에 포함되어 있음

# Data Type

### 강타입 언어

- 변수를 선언할 때 Data Type을 정의
- ↔ 약타입 언어

### 데이터 타입

- 클래스 이름은 pascal case `ex) MyFriend`
- 일반 변수 이름은 camel case  `ex) myFriend`
- java의 데이터 타입은 크게 2가지
    1. primitive data type
        
        해당 타입으로 선언된 변수 안에 직접 데이터가 저장
        
        ex) byte, short, **int**, long, float, **double**, char, boolean
        
    2. reference data type (class)
        
        클래스로 선언된 변수를 지칭
        
        해당 타입으로 선언된 변수 안에는 메모리 주소(해쉬값)가 저장
        
- 상수 : final로 선언 가능, snake case

### JVM 메모리 구조
![image](https://github.com/user-attachments/assets/0b71755f-9d25-4db5-b943-c92ed84f809a)

### 형변환

```java
// 명시적 형변환 (casting)
double phi = 3.14159265358979;
int myData = (int) phi;

// 묵시적 형변환 (implicit)
double k = 10 + 20.0;
```

# 객체 지향

```java
package mypackage;

// 1 -> 4 -> 5 -> 2 -> 3
public class InstanceTest {
    // static field or static variable or class variable
    // method area에 생성
    static int a = staticCall("1번");

    // instance variable
    int b = staticCall("2번");

    // static method
    static public int staticCall(String msg) {
        System.out.println(msg);
        return 100;
    }

    // constructor
    // 기본생성자는 무조건 작성을 하는게 좋음
    // 생성자는 인스턴스를 생성하기 위해 반드시 호출되고 만들어진 인스턴스를 초기화하는 역할
    public InstanceTest() {
        // this : 자기 자신의 method를 가지고 있는 instance
        this.b = staticCall("3번");
    }

    public static void main(String[] args) {
        System.out.println("4번");
        int c = staticCall("5번");
        InstanceTest d = new InstanceTest();
    }
}

```

## Overloading

같은 이름이나 같은 형태로 여러 개의 기능을 수행하는 경우

- 연산자 오버로딩
- 메서드 오버로딩
- 생성자 오버로딩
