# 학습 날짜 : 2025년 02월 11일
## JDBC JavaFX

### Event 처리

1. event source (event 발생 객체)
2. event 객체 (자동 생성) (event 세부 정보를 담고 있음)
3. event 처리 객체 (handler) (event 객체를 통해 처리)

⇒ **Event Delegation Model**

### VO, DO, ENTITY, DTO

- `DO` (= Domain Object) : Domain에서 사용하는 데이터를 객체화 시킨 것
- `VO` (= Value Object) : 각 table의 하나의 row를 담을 수 있는 객체
- `Entity` : VO와 유사 +  **Primary key**를 포함
- `DTO` (= Data Transfer Object) : VO와 유사

## JDBC MVC Pattern

### Layered Architecture

1. Presentation Layer - 입출력 처리
2. Business (Logic) Layer - 로직 처리
3. Persistence Layer - DB 접근
4. Database Layer - oracle, mysql …

### MVC Pattern

- Model
    - Data Model: VO
    - Service Model: 로직 처리 전담 객체
        - 순수 로직 처리
        - DB 처리 ⇒ DAO로 분리
- View
    - 입출력 담당 (presentation layer)
- Controller
    - View와 Model을 연결해주는 역할
