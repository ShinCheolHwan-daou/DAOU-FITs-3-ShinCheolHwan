## 학습 날짜 : 2025년 02월 28일
## 어떤 문제가 있었는지
DB 데이터를 모두 제 컴퓨터에 몰아서 저장하고 있었고 해당 데이터를 dump 떠서 조원분들께 나눠드려야 했습니다.

## 그 문제를 해결하기 위해 내가 시도해본 것들
ORACLE에서 DB dump 뜨는 방법을 검색했고, csv로 export하는 방법과 ORACLE 자체적으로 제공하는 방법이 있었습니다.

## 어떻게 그 문제를 해결했는지
ORACLE에서 제공하는 방법을 사용했습니다.
1. DB export
```powershell
expdp {exportUsername}/{exportUserpassword}@//localhost:1521/XE schemas={exportUsername} directory=DATA_PUMP_DIR dumpfile=my_dump.dmp logfile=my_dump.log
```
2. DB import
```powershell
impdp {importUsername}/{importUserpassword}@//localhost:1521/XE remap_schema={exportUsername}:{importUsername} directory=DATA_PUMP_DIR dumpfile=my_dump.dmp logfile=my_import.log
```

## 오늘 내가 새롭게 배운 것들
이전 Mysql을 사용할 때는 dump 뜨는데 오래 걸렸는데 ORACLE에서 제공하는 방법은 빨라서 좋았습니다.
