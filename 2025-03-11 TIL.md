## 어떤 문제가 있었는지
포인터의 개념에 대해 자세히 알지 못했습니다.

## 그 문제를 해결하기 위해 내가 시도해본 것들
구글링을 통해 포인터의 메모리 구조에 대해 그림으로 이해했고, 실제 코드로 찍어봤습니다.

## 어떻게 그 문제를 해결했는지
다음과 같이 다중 포인터를 만들어서 실제 주소값을 비교해보면서 이해도를 향상시켰습니다.
```c
#include <stdio.h>

int main(void) {
    int score = 100;
    int *pscore, **ppscore, ***pppscore;
    pscore = &score;
    ppscore = &pscore;
    pppscore = &ppscore;

    printf("포인터 변수 *pscore의 값은 %d\n", *pscore);
    printf("포인터 변수 **ppscore의 값은 %d\n", **ppscore);
    printf("포인터 변수 ***pppscore의 값은 %d\n", ***pppscore);
    printf("===========================================\n");
    printf("int 변수 score의 주소는 %p\n", &score);
    printf("포인터 변수 pscore의 값는 %p\n", pscore);
    printf("===========================================\n");
    printf("포인터 변수 pscore의 주소는 %p\n", &pscore);
    printf("포인터 변수 ppscore의 값는 %p\n", ppscore);
    printf("===========================================\n");
    printf("포인터 변수 ppscore의 주소는 %p\n", &ppscore);
    printf("포인터 변수 pppscore의 값는 %p\n", pppscore);
}

```

## 오늘 내가 새롭게 배운 것들
포인터의 개념과 실제값, &, * 등에 어떤 값이 저장되는지에 대해 배웠습니다.
