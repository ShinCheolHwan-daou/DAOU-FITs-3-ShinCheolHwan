# 2025.01.20 TIL
### Module 4 SQL 중급

1. 서브쿼리
   - SQL문 안에 포함된 또 다른 SQL문
   - 연관 여부에 따른 분류
     - 비연관 서브쿼리
       - 서브쿼리가 독립적으로 실행되어 결과를 반환
       - 메인 쿼리와 서브쿼리 간 상호작용 없음
     - 연관 서브쿼리
       - 서브쿼리가 메인 쿼리의 각 행마다 실행
       - 메인 쿼리의 데이터를 서브쿼리에서 참조
   - 위치에 따른 분류
     - 스칼라 서브쿼리
       - 서브쿼리가 컬럼 값으로 반환되어 SELECT 절에서 사용
       - 주로 집계 값이나 계산 결과를포함할 때 활용
     - 인라인 뷰
       - FROM 절에서 서브쿼리 결과를 테이블처럼 사용
       - 별칭을 붙여 활용 가능
     - 중첩 서브쿼리
       - WHERE 절에서 조건을 설정하기 위해 서브쿼리 사용
       - 서브쿼리 결과에 따라 메인 쿼리의 필터링 결정
       - HAVING 절에서 조건을 설정하기 위해 집계 함수와 함꼐 활용
   - 반환값 유형에 따른 분류
     - 단일 행 서브쿼리
       - 서브쿼리가 단일 행과 단일 열을 반환
       - 비교 연산자와 함께 사용
     - 다중 행 서브 쿼리
       - 서브쿼리가 여러 행을 반환
       - 다중 행 연산자와 함께 사용
       - 서브쿼리가 여러 열을 반환
       - 메인 쿼리의 여러 열과 비교 가능
   -  주의사항
       - 성능 최적화
         - 비연관 서브쿼리는 독립적으로 실행되므로 더 빠름
         - 연관 서브쿼리는 메인 쿼리의 각 행마다 실행되므로 성능 저하 가능
       - 적절한 반환값 확인
         - 서브쿼리가 반환하는 값의 유형에 따라 연산자 선택이 중요
       - JOIN 대체 가능성
         - 일부 서브쿼리는 JOIN으로 대체할 수 있어 성능 개선 가능
```MYSQL
-- 비연관 서브쿼리
SELECT NAME
FROM EMPLOYEES
WHERE SALARY > (SELECT AVG(SALARY) FROM EMPLOYEES);

-- 연관 서브쿼리
SELECT NAME FROM EMPLOYEES E1
WHERE SALARY = (
    SELECT MAX(SALARY)
    FROM EMPLOYEES E2
    WHERE E1.DEPARTMENT_ID = E2.DEPARTMENT_ID
);

-- 스칼라 서브쿼리
-- 캐싱 기능 존재
SELECT 
    NAME, 
    SALARY, 
    (SELECT AVG(SALARY) FROM EMPLOYEES WHERE DEPARTMENT_ID = E.DEPARTMENT_ID) AS AVG_SALARY
FROM EMPLOYEES E;

-- 인라인 뷰
SELECT
    D.DEPARTMENT_NAME,
    T.AVG_SALARY
FROM DEPARTMENTS D,
    (SELECT DEPARTMENT_ID, AVG(SALARY) AS AVG_SALARY FROM EMPLOYEES GROUP BY DEPARTMENT_ID) T
WHERE D.DEPARTMENT_ID = T.DEPARTMENT_ID;

-- 중첩 서브쿼리
SELECT NAME FROM EMPLOYEES WHERE DEPARTMENT_ID = (
    SELECT DEPARTMENT_ID FROM DEPARTMENTS WHERE DEPARTMENT_NAME = 'Department 1'
);

SELECT 
    DEPARTMENT_ID,
    SUM(SALARY) AS TOTAL_SALARY
FROM EMPLOYEES
GROUP BY DEPARTMENT_ID
HAVING SUM(SALARY) > (
    SELECT AVG(SUM(SALARY)) FROM EMPLOYEES GROUP BY DEPARTMENT_ID
);

-- 단일 행 서브쿼리
SELECT 
    NAME 
FROM EMPLOYEES WHERE SALARY = (SELECT MAX(SALARY) FROM EMPLOYEES);

-- 다중 행 서브쿼리
SELECT NAME 
FROM EMPLOYEES
WHERE DEPARTMENT_ID IN (
    SELECT DEPARTMENT_ID
    FROM DEPARTMENTS
    WHERE LOCATION = 'New York'
);

SELECT NAME
FROM EMPLOYEES
WHERE (DEPARTMENT_ID, JOB_ID) IN (
    SELECT DEPARTMENT_ID, JOB_ID
    FROM JOB_HISTORY
);

-- 서브쿼리 예제
SELECT ACCOUNT_ID, BALANCE
FROM ACCOUNTS
WHERE BALANCE = (SELECT MAX(BALANCE) FROM ACCOUNTS);

SELECT NAME, CREATED_AT
FROM CUSTOMERS
WHERE CREATED_AT = (SELECT MAX(CREATED_AT) FROM CUSTOMERS);

SELECT ACCOUNT_ID, BALANCE
FROM ACCOUNTS
WHERE BALANCE > (SELECT AVG(BALANCE) FROM ACCOUNTS);

SELECT BRANCH_ID, LOAN_ID, AMOUNT
FROM LOANS L1
WHERE AMOUNT = (
    SELECT MAX(AMOUNT)
    FROM LOANS L2
    WHERE L1.BRANCH_ID = L2.BRANCH_ID
);

SELECT ACCOUNT_ID, TRANSACTION_ID, AMOUNT
FROM TRANSACTIONS T1
WHERE AMOUNT = (
    SELECT MAX(AMOUNT)
    FROM TRANSACTIONS T2
    WHERE T1.ACCOUNT_ID = T2.ACCOUNT_ID
);
```
2. 집합연산자
   - SQL에서 두 개 이상의 SELECT 결과를 결합하여 하나의 결과 집합을 생성
   - 데이터베이스에서 집합 이론을 기반으로 작동
   - 서로 다른 쿼리의 결과를 비교하거나 결합할 때 유용
   - UNION : 두 SELECT 결과를 합치되 중복 제거
   - UNION ALL : 두 SELECT 결과를 합치되 중복 포함
   - INTERSECT : 두 SELECT 결과의 교집합
   - MINUS : 첫 번째 SELECT 결과에서 두 번째 SELECT 결과를 뺀 차집합
```MYSQL
-- 집합연산자 예제
SELECT CUSTOMER_ID FROM CUSTOMERS
INTERSECT
SELECT CUSTOMER_ID FROM LOANS;

SELECT DISTINCT CUSTOMER_ID FROM LOANS;

SELECT TRANSACTION_TYPE FROM TRANSACTIONS
UNION
SELECT ACCOUNT_TYPE FROM ACCOUNTS;
```
3. 윈도우 함수
   - SQL에서 행을 기준으로 특정 범위를 정의하여 작업하는 함수
   - 집계 함수와 함께 데이터를 그룹핑하지 않고도 계산 가능
4. 윈도우 순위 함수
   - RANK : 순위를 계산하며 동일한 값을 같은 순위를 부여
   - DENSE_RANK : 순위를 계산하며 동일한 값은 같은 순위를 부여
   - ROW_NUMBER : 순위를 고유하게 매김
```MYSQL
-- RANK 함수 예제
SELECT EMPLOYEE_ID, NAME, SALARY,
    RANK() OVER (ORDER BY SALARY DESC NULLS LAST) AS rank
FROM EMPLOYEES;

SELECT EMPLOYEE_ID, NAME, SALARY,
    RANK() OVER (ORDER BY SALARY DESC) AS rank
FROM EMPLOYEES
WHERE SALARY IS NOT NULL;

SELECT * FROM (
    SELECT DEPARTMENT_ID, EMPLOYEE_ID, NAME, SALARY,
        RANK() OVER (PARTITION BY DEPARTMENT_ID ORDER BY SALARY DESC) AS rank
    FROM EMPLOYEES
    WHERE SALARY IS NOT NULL AND DEPARTMENT_ID IS NOT NULL
)
WHERE rank = 1;

-- DENSE_RANK 함수 예제
SELECT EMPLOYEE_ID, NAME, SALARY,
    DENSE_RANK() OVER (ORDER BY SALARY DESC) AS dense_rank
FROM EMPLOYEES;

-- ROW_NUMBER 함수 예제
SELECT EMPLOYEE_ID, NAME, SALARY,
    ROW_NUMBER() OVER (ORDER BY SALARY DESC) AS row_number
FROM EMPLOYEES;
```
5. 윈도우 집계 함수
```MYSQL
-- AVG 함수 예제
SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
    AVG(SALARY) OVER (PARTITION BY DEPARTMENT_ID) AS DEPT_AVG
FROM EMPLOYEES;

-- MAX 함수 예제
SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY, 
    MAX(SALARY) OVER (PARTITION BY DEPARTMENT_ID) AS DEPT_MAX
FROM EMPLOYEES;

-- MIN 함수 예제
SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
    MIN(SALARY) OVER (PARTITION BY DEPARTMENT_ID) AS DEPT_MIN
FROM EMPLOYEES;

-- COUNT 함수 예제
SELECT EMPLOYEE_ID, DEPARTMENT_ID,
    COUNT(EMPLOYEE_ID) OVER (PARTITION BY DEPARTMENT_ID) AS DEPT_COUNT
FROM EMPLOYEES;
```
