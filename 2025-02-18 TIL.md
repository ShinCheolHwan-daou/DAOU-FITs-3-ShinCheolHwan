# 학습 날짜: 2025년 02월 18일
## Servlet 기초

### Client

1. HTML의 내용 DOM 구조 생성 (tree)
2. CSS의 내용 CSS Object Model 생성
3. Render Tree 생성 → 화면에 rendering

### Session

- http 프로토콜은 stateless
1. Cookie
    - 데이터(문자열) + 유효 기간 + 유효 Domain
    - Client 웹 브라우저에 저장
    - 보안성 X, 문자열, 길이 제한 존재
2. Session
    - Cookie의 확장 버전
    - HttpSession class의 instance
    - WAS 안에 저장 (session 공간)
    - Cookie를 이용 (cookie에 session ID 저장)

## JSP (Jakarta Server Pages)
1. MVC pattern web programming
2. servlet, service, DAO, VO, view
3. MyBatis DB 처리
4. JSP로 view 처리
    1. sendRedirect
    2. requestDispatcher
5. http Session (사용자 Tracking)

⇒ round trip 방식 ( = server side rendering)

### 로그인 게시판 구현
- members table
    - ID
    - PASSWORD
    - NAME
- boards table
    - ID
    - TITLE
    - CONTENT
    - WRITER_ID
    - CREATED_AT
    - UPDATED_AT
    - DELETED_AT
mapper에서 resultMap 부분에 대해 해결하다가 완성 못했습니다.
