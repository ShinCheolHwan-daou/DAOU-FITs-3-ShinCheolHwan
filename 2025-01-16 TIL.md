# 2025.01.16 TIL
### Module 4 SQL 중급
다양한 QUERY를 실습했습니다.
```MYSQL
-- 1. 직원과 부서 정보 조회
SELECT
    E.NAME,
    D.LOCATION
FROM EMPLOYEES E, DEPARTMENTS D
WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID;

-- 2. 직원과 매니저 정보 조회
SELECT
    E1.NAME,
    E2.NAME AS MANAGER_NAME
FROM EMPLOYEES E1
LEFT OUTER JOIN EMPLOYEES E2
    ON E1.MANAGER_ID = E2.EMPLOYEE_ID;

-- 3. 직원과 급여 정보 조회
SELECT
    NAME,
    NVL(TO_CHAR(SALARY), 'N/A') AS SALARY
FROM EMPLOYEES;

-- 4. 특정 부서 직원 조회
SELECT
    E.NAME,
    E.SALARY
FROM EMPLOYEES E, DEPARTMENTS D
WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID
    AND D.LOCATION = 'Chicago';

-- 5. 특정 급여 범위 조회
SELECT
    E.NAME,
    E.SALARY,
    D.DEPARTMENT_NAME
FROM EMPLOYEES E, DEPARTMENTS D
WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID
    AND E.SALARY BETWEEN 5000 AND 10000;
    
    
-- 6. 최근 3개월 내 입사한 직원
SELECT 
    NAME,
    HIRE_DATE
FROM EMPLOYEES
WHERE HIRE_DATE >= ADD_MONTHS(SYSDATE, -3);

-- 7. 부서장 이름 조회
SELECT
    E.NAME,
    D.DEPARTMENT_NAME
FROM DEPARTMENTS D
LEFT OUTER JOIN EMPLOYEES E
ON D.MANAGER_ID = E.EMPLOYEE_ID;

-- 8. 직무별 직원 수 조회
SELECT
    JOB_ID,
    COUNT(*) AS TOTAL
FROM EMPLOYEES E
GROUP BY E.JOB_ID;

-- 9. 부서별 평균 급여
SELECT
    D.DEPARTMENT_NAME,
    CEIL(AVG(E.SALARY)) AS AVG_SALARY
FROM DEPARTMENTS D, EMPLOYEES E
WHERE D.DEPARTMENT_ID = E.DEPARTMENT_ID
GROUP BY D.DEPARTMENT_NAME;

-- 10. 직원의 총 급여 합계
SELECT
    SUM(SALARY) AS TOTAL_SALARY
FROM EMPLOYEES;

-- 11. 대출 정보와 고객 이름 조회
SELECT 
    L.AMOUNT,
    C.NAME
FROM CUSTOMERS C, LOANS L
WHERE C.CUSTOMER_ID = L.CUSTOMER_ID
ORDER BY L.AMOUNT DESC;

-- 12. 대출 상태별 고객 수
SELECT 
    STATUS,
    COUNT(DISTINCT CUSTOMER_ID)
FROM LOANS
GROUP BY STATUS;

-- 13. 직원 수가 가장 적은 부서
SELECT 
    *
FROM (
    SELECT
        E.DEPARTMENT_ID,
        COUNT(*) AS NUMS
    FROM EMPLOYEES E
    WHERE E.DEPARTMENT_ID IS NOT NULL 
    GROUP BY E.DEPARTMENT_ID
    ORDER BY NUMS
)
WHERE ROWNUM = 1;

SELECT 
    DEPARTMENT_ID,
    NUMS
FROM (
    SELECT
        E.DEPARTMENT_ID,
        COUNT(*) AS NUMS,
        RANK() OVER (ORDER BY COUNT(*) ASC) AS RK
    FROM EMPLOYEES E
    WHERE E.DEPARTMENT_ID IS NOT NULL 
    GROUP BY E.DEPARTMENT_ID
)
WHERE RK = 1;

-- 14. 월별 신규 고객 수
SELECT
    TO_CHAR(C.CREATED_AT, 'YYYY-MM'),
    COUNT(*)
FROM CUSTOMERS C
GROUP BY TO_CHAR(C.CREATED_AT, 'YYYY-MM')
ORDER BY TO_CHAR(C.CREATED_AT, 'YYYY-MM') DESC;

-- 15. 상품 카테고리 계층 구조 조회
SELECT
    SH2.CATEGORY_NAME AS PARENT_CATEGORY_NAME,
    SH1.CATEGORY_NAME
FROM SHOPPING_CATEGORIES SH1
LEFT OUTER JOIN SHOPPING_CATEGORIES SH2
    ON SH1.PARENT_CATEGORY_ID = SH2.CATEGORY_ID;

-- 16. 특정 지점의 대출 합계
SELECT
    FLOOR(SUM(L.AMOUNT))
FROM BRANCHES B, LOANS L
WHERE B.BRANCH_ID = L.BRANCH_ID
    AND B.NAME = 'Branch 8';

-- 17. 직무별 직원의 평균 급여와 급여 등급 조회
SELECT
    E.JOB_ID,
    AVG(E.SALARY),
    CASE
        WHEN AVG(E.SALARY) >= 5000 AND AVG(E.SALARY) < 6000 THEN 'LOW'
        WHEN AVG(E.SALARY) >= 6000 AND AVG(E.SALARY) < 8000 THEN 'MEDIUM'
        WHEN AVG(E.SALARY) >= 8000 THEN 'HIGH'
    END AS SALARY_LEVEL
FROM EMPLOYEES E
GROUP BY E.JOB_ID
HAVING AVG(E.SALARY) >= 5000
ORDER BY AVG(SALARY) DESC;

-- 18. 고객별 거래 내역
SELECT
    C.NAME,
    SUM(T.AMOUNT)
FROM TRANSACTIONS T
INNER JOIN ACCOUNTS A
    ON T.ACCOUNT_ID = A.ACCOUNT_ID
INNER JOIN CUSTOMERS C
    ON A.CUSTOMER_ID = C.CUSTOMER_ID
GROUP BY C.NAME
ORDER BY C.NAME;

SELECT
    C.NAME,
    SUM(T.AMOUNT)
FROM TRANSACTIONS T
INNER JOIN ACCOUNTS A
    ON T.ACCOUNT_ID = A.ACCOUNT_ID
INNER JOIN CUSTOMERS C
    ON A.CUSTOMER_ID = C.CUSTOMER_ID
GROUP BY C.NAME
ORDER BY TO_NUMBER(REGEXP_SUBSTR(C.NAME, '[0-9]+$'));

-- 19. 부서별 직원의 평균 입사일 차이
SELECT 
    DEPARTMENT_NAME,
    ROUND(AVG(TRUNC(SYSDATE, 'DD') - TRUNC(E.HIRE_DATE, 'DD')))
FROM EMPLOYEES E, DEPARTMENTS D
WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID
GROUP BY DEPARTMENT_NAME
ORDER BY DEPARTMENT_NAME;

SELECT 
    DEPARTMENT_NAME,
    ROUND(AVG(TRUNC(SYSDATE, 'DD') - TRUNC(E.HIRE_DATE, 'DD')))
FROM EMPLOYEES E, DEPARTMENTS D
WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID
GROUP BY DEPARTMENT_NAME
ORDER BY TO_NUMBER(REGEXP_SUBSTR(DEPARTMENT_NAME, '[0-9]+$'));

-- 20. 대출 금액 상위 5명 고객 조회
SELECT 
    NAME,
    TOTAL_AMOUNT
FROM (
    SELECT
        C.NAME,
        C.CUSTOMER_ID,
        SUM(L.AMOUNT) AS TOTAL_AMOUNT
    FROM LOANS L, CUSTOMERS C
    WHERE L.CUSTOMER_ID = C.CUSTOMER_ID
    GROUP BY C.NAME, C.CUSTOMER_ID
    ORDER BY TOTAL_AMOUNT DESC, C.NAME ASC
)
WHERE ROWNUM <= 5;
```
