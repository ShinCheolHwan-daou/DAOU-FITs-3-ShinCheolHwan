## 학습 날짜 : 2025년 02월 24일
## 어떤 문제가 있었는지
금융권 관련 프로젝트를 시작했습니다.

## 그 문제를 해결하기 위해 내가 시도해본 것들
주제 탐색을 위해 다양한 자료들을 찾아봤습니다.

## 어떻게 그 문제를 해결했는지
그 중에서 게이미피케이션을 통해 MAU를 늘리는 전략에 대해 살펴봤고 키움증권에 존재하는 포인트 기능에 덧붙여서 추가될 수 있는 기능들을 생각해봤습니다.
그 결과, 1. 주식명과 관련된 꼬들, 2. 주식명과 관련된 꼬맨틀, 3. 주식 퀴즈, 4. 등락 예측 등의 게임을 기획했고 해당 게임 중에서 꼬들을 담당해서 구현하기로 결정했습니다.
오늘 ERD 설계와 API 설계를 완료한 상황입니다.
```SQL
CREATE TABLE WORDLE_WORDS
(
    ID               NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    FULL_TEXT        VARCHAR2(255),
    CHARACTERS       VARCHAR2(255),
    CHARACTER_LENGTH NUMBER,
    CAN_ANSWER       NUMBER(1),
    CREATED_AT       TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_AT       TIMESTAMP DEFAULT SYSTIMESTAMP,
    DELETED_AT       TIMESTAMP DEFAULT NULL
);

CREATE TABLE WORDLE_QUIZZES
(
    ID          NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    WORD_ID     NUMBER,
    STARTED_AT  TIMESTAMP,
    FINISHED_AT TIMESTAMP,
    CREATED_AT  TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_AT  TIMESTAMP DEFAULT SYSTIMESTAMP,
    DELETED_AT  TIMESTAMP DEFAULT NULL
);

CREATE TABLE WORDLE_TRIALS
(
    ID         NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    QUIZ_ID    NUMBER,
    UUID       NUMBER,
    CHARACTERS VARCHAR2(255), -- ㅇ,ㅣ,ㄴ,ㅅ,ㅡ,ㅌ,ㅏ 로 표시
    STRIKES    VARCHAR2(255), -- 1, 0, 0, 1,0,0,0 으로 표시
    BALLS      VARCHAR2(255), -- 1, 0, 0, 1,0,0,0 으로 표시
    NONES      VARCHAR2(255), -- 1, 0, 0, 1,0,0,0 으로 표시
    IS_ANSWER  NUMBER(1),
    CREATED_AT TIMESTAMP DEFAULT SYSTIMESTAMP,
    UPDATED_AT TIMESTAMP DEFAULT SYSTIMESTAMP,
    DELETED_AT TIMESTAMP DEFAULT NULL
);
```

## 오늘 내가 새롭게 배운 것들
꼬들이라는 게임을 설계해보면서 어떻게 구조가 잡혀야되는지 고민할 수 있는 시간이였습니다.
