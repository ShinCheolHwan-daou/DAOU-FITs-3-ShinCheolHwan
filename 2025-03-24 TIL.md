## 어떤 문제가 있었는지
C언어의 전처리기와 모듈화에 대해 배우고, 해당 문제를 풀었습니다.

## 그 문제를 해결하기 위해 내가 시도해본 것들
전처리기와 모듈화에 대해 구글링과 강의 내용을 참고해서 문제를 해결했습니다.

## 어떻게 그 문제를 해결했는지
```C
// - 일반 사용자(USER_NORMAL)은 최대 거래한도가 1,000,000원입니다.
// - VIP 사용자(USER_VIP)는 최대 거래 한도가 10,000,000원입니다
// - 환경이 정의 되지 않으면 기본 한도는 5,000,000원으로 설정됩니다
// - #define USER_NORMAL 또는 #define USER_VIP를 이용해서 사용자의 등급을 변경할 수 있습니다.
#include <stdio.h>

// #define USER_NORMAL
// #define USER_VIP

#ifdef USER_NORMAL
#define USER_GRADE "normal"
#define USER_LIMIT 1000000
#elifdef USER_VIP
#define USER_GRADE "vip"
#define USER_LIMIT 10000000
#else
#define USER_GRADE "undefined"
#define USER_LIMIT 5000000
#endif

int main(void) {
    printf("grade: %s, limit: %d", USER_GRADE, USER_LIMIT);
}
```

## 오늘 내가 새롭게 배운 것들
#### 전처리기
- #define	매크로 정의 (ex: #define PI 3.14)
- #include	외부 파일 포함 (ex: #include <stdio.h>)
- #ifdef / #ifndef	특정 매크로가 정의되었는지 / 안 되었는지 체크
- #if / #elif / #else / #endif	조건부 컴파일 수행
- #pragma	컴파일러에 특정 옵션을 전달
#### 모듈화
- 헤더 파일 (.h) → 함수, 변수 선언
- 소스 파일 (.c) → 함수 정의
- 메인 파일 (main.c) → 프로그램 실행
