## 어떤 문제가 있었는지
C언어에서 연산자와 반복문을 활용하여 코드를 구현하였습니다.

## 그 문제를 해결하기 위해 내가 시도해본 것들
그 과정에서 연산자 우선 순위에 대해 모르는 내용이 있어서 구글링을 통해 해결했습니다.

## 어떻게 그 문제를 해결했는지
구글링을 통해 연산자 우선순위에 대해 공부하고 다음과 같은 코드를 짤 수 있었습니다.
```C
#include <stdio.h>
#define _USE_MATH_DEFINES
#include <math.h>

void p1_1(void);
void p1_2(void);
void p1_3(void);
void p1_4(void);
void p1_5(void);
void p1_6(void);
void p1_7(void);
void p1_8(void);
void p1_9(void);
void p1_10(void);

void p2_19(void);
void p2_20(void);

void p3_2(void);
void p3_4(void);
void p3_6(void);
void p3_9(void);

void control1(void);
void control2(void);
void control3(void);
void control4(void);
void control5(void);
void control6(void);
void control7(void);

int main(void)
{
	// p1_1();
	// p1_2();
	// p1_3();
	// p1_4();
	// p1_5();
	// p1_6();
	// p1_7();
	// p1_8();
	// p1_9();
	// p1_10();

	// p2_19();
	// p2_20();

	// p3_2();
	// p3_4();
	// p3_6();
	// p3_9();

	// control1();
	// control2();
	// control3();
	// control4();
	// control5();
	// control6();
	// control7();
	return 0;
}

void p1_1(void)
{
	// 	문1 연산자 = 와 == 의 차이점은 ?
	//	답: = 대입, == 비교
}

void p1_2(void)
{
	//	문2 다음 연산자를 우선순위가 높은 순서대로 정리하시오
	//	답:	증감 산술 관계 비트 논리 조건 대입 콤마
}

void p1_3(void)
{
	//	문3 다음의 코드를 실행 후 결과값은 ?
	//	3번-1
	//	#include <stdio.h>
	// 		int main()
	// 	{
	// 		int x = 3, y = 4;
	// 		printf("%d\n", ++x * 2);
	// 		printf("%d\n", y-- % 5);
	// 		return 0;
	// 	}
	//	답: 8 4
	//
	// 	//3번-2
	// 	#include <stdio.h>
	// 	int main()
	// 	{
	// 	int x = 15, y = 21;
	// 	printf("%d\n", x & y);
	// 	printf("%d\n", x | y);
	// 	printf("%d\n", x ^ y);
	// 	return 0;
	// 	}
	//	풀이: 01111
	//		  10101
	//  답: 5 31 26

	// 	3번-3
	// 	#include <stdio.h>
	// 	int main()
	// 	{
	// 		int x, y, z;
	// 		x = -3 * 5 % 5 +1;
	// 		y = 1;
	// 		z = 1;
	// 		y = x && y || z;
	// 		z += x << 1 + y++;
	// 		printf("x = %d\n", x);
	// 		printf("y = %d\n", y);
	// 		printf("z = %d\n", z);
	// 		return 0;
	// 	}
	//	답: x = 1 y = 2 z = 5
	//
	// 	//3번-4
	// 	#include <stdio.h>
	// 	int main()
	// 	{
	// 		int x = 5, y;
	// 		double z;
	// 		y = x/2;
	// 		printf("y = %d\n", y);
	// 		y = (double)x/2;
	// 		printf("y = %d\n", y);
	// 		z = x/2;
	// 		printf("z = %.1lf\n", z);
	// 		z = (double)x/2;
	// 		printf("z = %.1lf\n", z);
	// 		z = (double)(x/2);
	// 		printf("z = %.1lf\n", z);
	// 		return 0;
	// 	}
	//	답: y = 2 y = 2 z = 2.0 z = 2.5 z = 2.0
}

void p1_4(void)
{
	// 	문4 킬로그램 단위의 무게를 입력받아 파운드(lb)단위로 출력하는 프로그램을 작성하시오 소수 1자리
	// 		힌트 1l = 0.45359237kg
	// 		실행 결과
	// 		kg단위의 무게를 입력하세요 : 100
	// 		입력하신 무게 100.0kg은 220.5lb에 해당합니다
	double kg;
	printf("kg단위의 무게를 입력하세요 : ");
	scanf_s("%lf", &kg);
	getchar();
	printf("입력하신 무게 %.1lfkg은 %.1lflb에 해당합니다.\n", kg, kg / 0.45359237);
}

void p1_5(void)
{
	//	문5 원기둥의 밑면의 반지름과 높이를 입력받아 부피를 계산하는 프로그램을 작성하시오
	// 		(소수 1자리)
	// 		힌트 원기둥의 부피 = 밑면의 넓이 * 높이 = (파이 * 반지름 * 반지름 * 높이)
	// 		실행결과
	// 		원기둥의 밑면의 반지름 높이를 입력하세요(cm 단위) : 3.5
	// 		계산한 원기둥의 부피는 141.4cm입니다
	double r, h;
	printf("원기둥의 밑면의 반지름 높이를 입력하세요(cm 단위) : ");
	scanf_s("%lf %lf", &r, &h);
	printf("계산한 원기둥의 부피는 %.1lfcm입니다\n", M_PI * r * r * h);
}

void p1_6(void)
{
	//  문6 키보드로 월을 입력 받아 입력 받은 월이 상반지인지 하반지인지 판단하여 출력하는 프로
	// 		그램을 작성하시오. (힌트 논리 연산자와 조건연산자를 사용하세요)
	// 		결과화면
	// 		월(1~12) 입력 : 7
	// 		7월은 하반기입니다.
	int month;
	printf("월(1~12) 입력 : ");
	scanf_s("%d", &month);
	printf("%d월은 %s입니다.", month, month > 6 ? "하반기" : "상반기");
}

void p1_7(void)
{
	//  문7 기가바이트 수치를 입력받아 입력한 값이 몇 메가바이트인지, 몇 킬로바이트인지 계산하
	// 		는 프로그램을 작성하시오.힌트 1기가바이트 = 1메가바이트 * 1024
	// 		1메가바이트 = 1킬로바이트 * 1024
	// 		결과화면
	// 		입력(기가바이트) : 30
	// 		30기가바이트 = 30720메가바이트 = 31457280킬로바이트
	int gigaByte;
	printf("입력(기가바이트) : ");
	scanf_s("%d", &gigaByte);
	printf("%d기가바이트 = %d메가바이트 = %d킬로바이트\n", gigaByte, gigaByte * 1024, gigaByte * 1024 * 1024);
}

void p1_8(void)
{
	// 문8 초 단위의 시간을 입력 받아 몇시간 몇분 몇초인지를 계산하는 프로그램을 작성하시오
	// 	실행결과
	// 	초단위 입력 12340
	// 	입력한 12340초는 3시간 25분 40초입니다
	int second;
	printf("초단위 입력 ");
	scanf_s("%d", &second);
	printf("입력한 %d초는 %d시간 %d분 %d초입니다\n", second, second / 60 / 60, second % 3600 / 60, second % 3600 % 60);
}

void p1_9(void)
{
	// 	문9 다음 수식을 계산하는 프로그램 작성하시오
	// 	f(x) - 2x ^ 3 – 3x ^ 2 + 1 / x
	// 	x입력 : 3
	// 	x가 3일 때 값은 27.3입니다
	int x;
	printf("x입력 :");
	scanf_s("%d", &x);
	printf("x가 %d일 때 값은 %.1lf입니다\n", x, 2.0 * x * x * x - 3.0 * x * x + 1.0 / x);
}

void p1_10(void)
{
	// 	문10 조건부 연산자를 이용해서 정수 3개를 입력 받아 가장 큰 수를 출력하세요
	int numbers[3] = {0, 0, 0};
	printf("정수 3개 입력 :");
	scanf_s("%d %d %d", &numbers[0], &numbers[1], &numbers[2]);
	printf("최대값 %d\n", numbers[0] >= numbers[1] && numbers[0] >= numbers[2]
		                   ? numbers[0]
		                   : numbers[1] >= numbers[0] && numbers[1] >= numbers[2]
		                   ? numbers[1]
		                   : numbers[2]);
}

void p2_19(void)
{
	// 19. 16비트의 부호 없는 정수형 데이터를 오른쪽으로 1비트 이동하면 오른쪽에 있는 비트가
	// 	제거됩니다.이때 제거되는 비트를 왼쪽의 최상위 비트에 채워 넣은 오른족 로테이션(right
	// 	rotation)프로그램을 작성하시오
	unsigned short a = 0;
	printf("16비트의 부호 없는 정수형 데이터 : ");
	scanf_s("%hd", &a);
	printf("%4x -> ", a);
	a = (a >> 1) | ((a & 1) << 15);

	for (int i = 0; i < 16; i++)
	{
		if (i != 0 && i % 4 == 0)
		{
			printf(" ");
		}
		printf("%d", ((a >> (15 - i)) & 1));
	}
	printf("\n");
}

void p2_20(void)
{
	int a, b, c, d, min, max;
	printf("정수 4개 입력: ");
	scanf_s("%d %d %d %d", &a, &b, &c, &d);

	max = a > b && a > c && a > d
		      ? a
		      : b > a && b > c && b > d
		      ? b
		      : c > a && c > b && c > d
		      ? c
		      : d;

	min = a < b && a < c && a < d
		      ? a
		      : b < a && b < c && b < d
		      ? b
		      : c < a && c < b && c < d
		      ? c
		      : d;

	printf("최대: %d\n", max);
	printf("최소: %d\n", min);
}

void p3_2(void)
{
	int a, b;
	printf("8진수, 16진수를 순서대로 입력하세요 : ");
	scanf_s("%o %x", &a, &b);
	printf("8진수 %o + 16진수 %x = %d\n", a, b, a + b);
}

void p3_4(void)
{
	double height, weight;
	printf("키를 입력하세요 ");
	scanf_s("%lf", &height);

	printf("몸무게를 입력하세요 ");
	scanf_s("%lf", &weight);

	printf(
		"당신의 키는 %.1lfcm(%.1lffeet)입니다.당신의 체중은 %.1lfkg(%.1lflb)입니다.표준 체중 %.1lf과의 차이는 %s %.1lfkg입니다.\n",
		height,
		height / 30.49,
		weight,
		weight * 2.20459,
		(height - 100) * 0.9,
		weight - (height - 100) * 0.9 > 0 ? "+" : "-",
		weight - (height - 100) * 0.9
	);
}

void p3_6(void)
{
	// 6. 입력받은 문자의 비트열 구하기
	// 	힌트
	// 	자료 비트열 B의 오른쪽에서 n번째 비트 값을 찾아서 출력하기
	// 	1단계 오른쪽에서 n번째 비트만 1이고 나머지는 0인 마스크 비트열 M구하기
	// 	M = (1 << n - 1)
	// 	2단계 B와 M에 대해 비트 AND 연산을 한 비트열 R 구하기 R = B & M
	// 	3단계 결과 비트열 R의 정수 값이 0이면 0을 0이 아니면 1을 출력하기
	// 	문자 입력 : a
	// 	아스키 코드값으로 97이므로
	// 	2진수로 0 1 1 0 0 00 1
	char inputChar;
	scanf_s("%c", &inputChar, sizeof(inputChar));

	for (int i = 0; i < 8; i++)
	{
		if (i != 0 && i % 4 == 0)
		{
			printf(" ");
		}
		printf("%d", ((inputChar >> (7 - i)) & 1));
	}
	printf("\n");
}

void p3_9(void)
{
	// 9. 학생의 이름(이름 사이에 띄어쓰기 가능)과 국어, 영어, 수학, 출결점수를 키보드로 입력 받
	// 	아 네 과목 점수의 평균(소수 1자리)을 구하고 합격 여부(평균 70점 이상이고 출결점수가 80
	// 		점 이상인 경우에만 합격)을 출력하는 프로그램을 작성하시오
	char name[10];
	float scores[4];
	printf("이름: ");
	gets_s(name, sizeof(name));
	printf("국어: ");
	scanf_s("%f", &scores[0]);
	printf("영어: ");
	scanf_s("%f", &scores[1]);
	printf("수학: ");
	scanf_s("%f", &scores[2]);
	printf("출결점수: ");
	scanf_s("%f", &scores[3]);

	printf("평균: %.1f\n", (scores[0] + scores[1] + scores[2] + scores[3]) / 4);
	printf("합격 여부 : %s\n", (scores[0] + scores[1] + scores[2] + scores[3]) / 4 >= 70 && scores[3] >= 80 ? "합격" : "불합격");
}


void control1(void)
{
	for (int i = 4; i >= 1; i--)
	{
		for (int j = 0; j < i; j++)
		{
			printf("*");
		}
		printf("\n");
	}
}

void control2(void)
{
	int a;
	while (1)
	{
		printf("숫자를 입력하세요 ");
		scanf_s("%d", &a);

		if (a == 999)
		{
			break;
		}
	}
}

void control3(void)
{
	printf("========================\n");
	printf("1. 원의 둘레 구하기\n");
	printf("2. 원의 넓이 구하기\n");
	printf("3. 구의 부피 구하기\n");
	printf("4. 그만두기\n");
	printf("========================\n");

	int menu, r;
	while (1)
	{
		printf("원하는 메뉴는 ? ");
		scanf_s("%d", &menu);

		switch (menu)
		{
		case 1:
			printf(">> 반지름은 ? ");
			scanf_s("%d", &r);
			printf(">> 반지름이 %d인 원의 둘레는 %.2f\n", r, 2 * M_PI * r);
			break;
		case 2:
			printf(">> 반지름은 ? ");
			scanf_s("%d", &r);
			printf(">> 반지름이 %d인 원의 넓이는 %.2f\n", r, r * r * M_PI);
			break;
		case 3:
			printf(">> 반지름은 ? ");
			scanf_s("%d", &r);
			printf(">> 반지름이 %d인 구의 부피는 %.2f\n", r, 4.0 / 3 * M_PI * r * r * r);
			break;
		default:
			return;
		}
	}
}

void control4(void)
{
}

void control5(void)
{
}

void control6(void)
{
}

void control7(void)
{
	int num, bit = 1;
	printf("정수 입력 ");
	scanf_s("%d", &num);

	for (int i = 31; i >= 0; i--)
	{
		if (num >> i & 1)
		{
			bit = i + 1;
			break;
		}
	}

	printf("유효 비트: %d\n", bit);
}

```

## 오늘 내가 새롭게 배운 것들
C언어에서 연산자와 반복문을 활용하여 코드를 구현하였습니다. 
