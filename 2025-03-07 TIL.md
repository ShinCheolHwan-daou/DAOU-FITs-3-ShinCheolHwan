## 어떤 문제가 있었는지
가변인자를 처리하는데 컴파일러와 운영체제에 dependency가 없는 pointer 연산을 하는데 어려움을 느꼈습니다.

## 그 문제를 해결하기 위해 내가 시도해본 것들
구글링을 통해 자료 검색을 했습니다.

## 어떻게 그 문제를 해결했는지
stdarg.h에서 va_list, va_start, va_end를 활용해서 해결했습니다.

## 오늘 내가 새롭게 배운 것들
새로운 함수를 사용해봤습니다.
```c
int sum(int count, int val, ...) {
    va_list args;
    int total = val; // 첫 번째 값(val)을 초기값으로 설정

    va_start(args, val); // val 이후의 가변 인자 시작

    // count-1개의 가변 인자 가져오기
    for (int i = 1; i < count; i++) {
        int test = va_arg(args, int);
        total += test;
    }

    va_end(args); // 가변 인자 종료
    return total;
}
```
