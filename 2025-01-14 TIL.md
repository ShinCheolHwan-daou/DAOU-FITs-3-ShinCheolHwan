# 2025.01.14 TIL
## 데이터베이스개론 SQL
### Module 2. DB 모델링
**정규화**
1. 정규화
   - 데이터 베이스의 설계 단계에서 데이터 중복을 제거하고 데이터 무결성을 유지하기 위한 과정
   - 데이터를 논리적으로 분리하여 효율적으로 저장하고 관리하도록 설계
   - 데이터 중복 방지
   - 데이터 일관성 및 무결성 유지
   - 데이터 저장소의 효율성 향상
   - 데이터 변경 시 발생할 수 있는 문제 최소화 (삽입 이상, 삭제 이상, 갱신 이상)
2. 제 1정규형
   - 모든 속성은 반드시 하나의 값만 가져야한다
3. 제 2정규형
   - 엔터티의 모든 일반속성은 반드시 모든 주식별자에 종속되어야 한다.
4. 제 3정규형
   - 주식별자가 아닌 모든 속성 간에는 서로 종속될 수 없다.
5. 반정규화
   - 데이터베이스 설계에서 정규화를 통해 분리된 테이블을 특정 성능 향상이나 시스템 요구에 따라 다시 결합하거나 중복 데이터를 허용하는 과정
   - 성능 최적화와 효율적인 데이터 액세스를 위해 데이터 모델을 조정하는 것
   - 정규화의 한계
     - 정규화를 통해 데이터 무결성과 중복 문제를 해결하지만, 다수의 테이블 조인이 필요한 쿼리에서는 성능 저하 발생 가능
     - 읽기 속도가 중요한 상황에서 정규화된 데이터 모델은 비효율적일 수 있음
   - 성능 최적화
     - 쿼리 성능을 개선하기 위해 데이터를 중복 저장하거나 테이블을 합치는 방식으로 읽기 속도를 높임
     - 주로 OLAP 시스템에서 활용
   - 반정규화 적용 방법
     - 속성 추가
     - 테이블 병합
     - 중복 데이터 저장
     - 이력 데이터 관리
   - 반정규화의 장점
     - 성능 개선
     - 간단한 쿼리
     - 특정 요구사항 대응
   - 반정규화의 단점
     - 데이터 중복
     - 데이터 무결성 문제
     - 유연성 감소
6. OLTP (=Online Transaction Processing)
   - 실시간으로 다수의 트랜잭션을 처리하는 시스템
   - 데이터 입력, 저장, 수정 및 조회를 빠르게 처리
   - ex) 은행 시스템, 전자상거래, 예약 시스템
7. OLAP (=Online Analytical Processing)
   - 대규모 데이터를 분석하고, 요약 정보와 통찰력을 제공하는 시스템
   - 데이터의 분석, 보고서 생성, 비즈니스 인사이트 제공
   - ex) 데이터 마트, 비즈니스 인텔리전스, 대시보드 시스템
8. OLTP와 OLAP의 상호작용
   - OLTP 시스템은 데이터 생성, OLAP 시스템은 데이터 활용
   - OLTP에서 생성된 데이터를 OLAP로 추출하여 분석
   - ETL 프로세스: 데이터 웨어하우스로 데이터를 전송하기 위해 ETL(Extract, Transform, Load) 사용
   - OLTP -> ETL -> OLAP의 흐름
9. 논리 모델과 물리 모델
    - 논리 모델: 데이터베이스 설계의 한 단계로 데이터의 구조와 관계를 기술하는 모델
    - 물리 모델: 데이터베이스 설계의 최종 단계로 특정 DBMS에 종속적인 데이터 구조를 정의하는 모델

**SQL의 개요**
1. SQL이란
   - 데이터베이스를 관리하고 조작하기 위한 언어
   - 관계형 데이터 베이스에서 데이터를 검색, 추가, 수정, 삭제하는 데 사용
   - 데이터 정의, 조작, 제어를 위한 명령어 집합 제공
2. DDL
   - 데이터베이스의 구조 정의
   - ex) CREATE, ALTER, DROP, TRUNCATE
3. DML
   - 데이터를 조작 (삽입, 수정, 삭제)
   - ex) SELECT, INSERT, UPDATE, DELETE
4. DCL
   - 데이터 접근 권한 제어
   - ex) GRANT, REVOKE
5. TCL
   - 트랜잭션 관리
   - COMMIT, ROLLBACK, SAVEPOINT

**트랜잭션**
1. 트랜잭션
   - 데이터베이스에서 하나의 논리적 작업 단위
   - 일련의 작업이 모두 성공하거나 실패하는 것을 보장
   - 트랜잭션은 데이터 무결서오가 일관성을 유지하기 위한 기본 단위로 사용
2. Atomicity (=원자성)
   - 트랜잭션은 모두 실행되거나, 모두 실행되지 않아야 함
   - 실패 시 변경 사항을 ROLLBACK하여 원래 상태로 복구
3. Consistency (=일관성)
   - 트랜잭션이 완료된 후 데이터베이스는 항상 일관된 상태를 유지해야 함
   - 데이터 무결성을 보장
4. Isolation (=고립성)
   - 여러 트랜잭션이 동시에 실행될 때 서로 간섭하지 않도록 독립적으로 처리
   - 트랜잭션 격리 수준으로 구현
   - ex) 0 : read uncommited (dirty read 발생)
   - 1 : read commited (commit된 것을 읽음)
   - 2 : repeatable read (선행 transaction이 읽은 데이터는 transaction이 끝날 때까지 접근 불가능, FOR UPDATE)
   - 3 : serializable (선행 transaction이 끝날 때까지 접근 불가)
5. Durability (=지속성)
   - 트랜잭션이 성공적으로 완료되면 병경된 데이터는 영구적으로 저장
   - 시스템 장애 후에도 데이터는 유지
6. 설계 시 유의 사항
   - 작업 단위를 명확히 정의
   - 최소한의 데이터 변경
   - 장기 트랜잭션 방지
   - 적절한 격리 수준 설정정

### Module 3. SQL 기초
**SELCET**
1. 실습
```MYSQL
-- 기본 문법
SELECT 컬럼명 FROM 테이블명;
-- 컬럼 ALIAS
SELECT 컬럼명 AS 별칭 FROM 테이블명;
-- 테이블 ALIAS
SELECT 컬럼명 FROM 테이블명 별칭;

-- NULL 비교 연산 무조건 FALSE
-- 가로 연산에서는 NULL (ex: 1 + NULL = NULL)
-- 세로 연산에서는 NULL 제외 (ex: 5개 평균 1개 NULL -> NULL 아닌 4개의 평균)
SELECT * FROM EMPLOYEES WHERE SALARY IS NULL;
SELECT * FROM EMPLOYEES WHERE SALARY = NULL;

-- AND, OR 동시에 존재하면 AND부터 실행
SELECT * FROM EMPLOYEES 
    WHERE DEPARTMENT_ID=10 AND SALARY > 5000 OR DEPARTMENT_ID=1;

-- LIKE문
SELECT * FROM FILES WHERE FILE_NAME LIKE 'Data\_2024%' ESCAPE '\';

-- BETWEEN 시작값 끝값 포함
SELECT * FROM EMPLOYEES WHERE SALARY BETWEEN 3000 AND 5000;
```
2. 이론
   - 연산자의 우선순위
     - 괄호, 비교 연산자, 논리 연산자 NOT, 논리 연산자 AND, 논리 연산자 OR
