## 어떤 문제가 있었는지
구조체 어레이를 선언하는 과정에서 초기화하는 방법을 몰랐습니다.

## 그 문제를 해결하기 위해 내가 시도해본 것들
```C
Book books[n];
books[0] = {1, 2, 3};
books[1] = {4, 5, 6};
```
초기에는 다음과 같이 선언해서 초기화하려고 시도했습니다.


## 어떻게 그 문제를 해결했는지
```C
Book books[n] {{1, 2, 3}, {4, 5, 6}};
```
다음과 같이 수정했습니다.

## 오늘 내가 새롭게 배운 것들
처음 시도한 방식으로 안되는 이유는 books를 선언하면서 이미 쓰레기 값으로 초기화가 진행되었기 때문입니다.<br>
따라서 선언과 동시에 함으로써 해결할 수 있었습니다.
