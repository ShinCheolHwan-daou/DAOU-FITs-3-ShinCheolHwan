# 2025.01.09 TIL
### Test 오답 노트
**7. 객체지향 접근방법에서 사용하는 다이어그램이 아닌 것은?**
- 가) 유즈케이스 다이어그램
- 나) 개체 관계 다이어그램
- 다) 시퀀스 다이어그램
- 라) 클래스 다이어그램

답: 나

풀이: 개체 관계 다이어그램은 구조적 접근 방법 중 하나이다.

**10. 다익스트라 알고리즘을 적용하여 A노드에서 출발하여 F로 가는 최단 경로와 거리를 구하시오.**

<img src="https://github.com/user-attachments/assets/5953777b-3577-4c06-9da7-463b58d9eec2" width="400px"/>

답: 최단 경로: A-D-C-E-F / 거리: 8

풀이: 현재 갈 수 있는 edge 중에서 가장 짧은 edge를 선택해 나간다.

### Module 3. IT 서비스 이해
**인공지능 원리와 사용**
1. 인공지능
   - 인간의 지각, 학습, 추론 능력을 인공적으로 구현하는 기술
   - 인간처럼 생각하는 기계를 만드는 것
   - 인공지능
     - 컴퓨터가 사람의 행동을 흉내내는 모든 기술들
   - 머신러닝
     - 회귀, 분류를 이용하여 기계가 학습하게 하는 이공지능 기술
   - 딥러닝
     - 다중층 신경망을 이용하는 머신러닝기술
   - 인공지능의 발전사
     - 튜링테스트 -> 규칙 기반 알고리즘 -> 전문가 시스템 -> 머신러닝 -> 딥러닝 -> 생성형 인공지능
2. 머신러닝
   - 알고리즘을 이용하여 데이터의 패턴을 학습하고, 학습한 내용을 기반으로 스스로 판단하는 인공지능 기술
   - 지도학습: 라벨(=y) 존재
     - ex) 분류 / 회귀
   - 비지도학습: 라벨(=y) 존재 X
     - ex) 군집
   - 강화학습: 보상 시스템을 이용하여 액션을 배움
     
3. 분류 알고리즘
   - 서로 다른 그룹으로 나누는 기법
   - 로지스틱 회귀분석
     - 종속변수의 확률을 예측하는데 사용되는 분류 알고리즘
       ```python
       # 로지스틱 회귀
       from sklearn.linear_model import LogisticRegression
       from sklearn.metrics import accuracy_score, confusion_matrix, classification_report

       # 모델 훈련
       model = LogisticRegression()
       model.fit(X_train, y_train)

       # 예측
       y_pred = model.predict(X_test)

       # 성능 평가
       print(f"Accuracy: {accuracy_score(y_test, y_pred):.4f}")
       print(f"Confusion Matrix:\n{confusion_matrix(y_test, y_pred)}")
       print(f"Classification Report:\n{classification_report(y_test, y_pred)}")
       ```
   - 의사결정트리
     - 분류 문제에서 가장 많이 쓰이는 지도학습 방법
     - 변수들을 가장 중요한 분류기준을 바탕으로 비슷한 그룹으로 나눔
   - 랜덤 포레스트
     - 분류트리의 집합
     - 관측치와 변수의 부분집합을 모아서 의사결정트리를 만드는 앙상블기법
       ```python
       # 랜덤 포레스트
       from sklearn.ensemble import RandomForestClassifier

       # 모델 훈련
       rf_model = RandomForestClassifier()
       rf_model.fit(X_train, y_train)
       
       # 예측
       rf_pred = rf_model.predict(X_test)

       # 성능평가
       print(f"Accuracy: {accuracy_score(y_test, rf_pred):.4f}")
       print(f"Confusion Matrix:\n{confusion_matrix(y_test, rf_pred)}")
       print(f"Classification Report:\n{classification_report(y_test, rf_pred)}")
       ```
   - 선형판별분석
   - K-Nearest Neighbor
   - 지원벡터머신
   - 신경망
   - 부스팅
4. 회귀 알고리즘
   - 데이터를 표현하는 식을 찾아내는 기법
   - Clustering
     - 비슷한 개체들을 묶어주는 기법
5. 변수 스케일링
   - 정규화를 하면 각 변수가 결과에 미치는 영향을 비례적으로 알 수 있음
   - 정규화를 하지 않으면 제대로 작동하지 않은 목적함수가 있음
   - 정규화를 하면 결과에 빨리 도착함
   - Normalization(정규화): 두 숫자 사이의 값으로 변환하는 것
   - Standardization(표준화): 평균이 0이고 분산이 1인 값으로 변환하는 것
   - 최소 최대값 스케일러 -> 정규화 해주는 도구
   - 표준 스케일러 -> 표준화 해주는 도구
6. 딥러닝
   - 인간과 신경망을 모형화한 알고리즘에 방대한 데이터를 학습시켜 결과를 얻는 머신러닝 기술
   - 아주 많은 양의 데이터를 학습하기 위하여 여러 층의 인공신경망을 사용
   - 합성공 신경망 (=CNN)
     - 시각적 이미지를 분석하는데 사용되는 깊고 피드-포워드적인 신경망
7. 생성형 인공지능
   - 방대한 양의 데이터로 사전 훈련된 인공신경망 모델을 기반으로 학습하지 않은 콘텐츠를 만들어내는 인공지능
   - 새로운 데이터, 이미지, 음성, 또는 텍스트 등을 생성하는 기술
8. 초거대 언어모델(=LLM)
   - 방대한 양의 데이터를 기반으로 사전 학습된 딥 러닝 모델
   - 입력을 순차적으로 처리하는 RNN과 달리 전체 시퀀스를 병렬로 처리
   - 일반적인 상황에서 프롬프트에 응답하는데 탁월하나 훈련한 적이 없는 특정 영역에서는 성능이 떨어짐
9. 순환 인공 신경망(=RNN)
    - 신경망의 종류로 이전 결과물이 현재 스텝의 입력값이 됨
    - 연속된 데이터를 처리하기 가장 좋은 모델
    - ex) 자연어 처리
10. 랭체인
    - 대규모 언어 모델을 기반으로 애플리케이션을 구축하기 위한 오픈 소스 프레임워크
    - 검색을 통해 언어모델에 지식을 보완
