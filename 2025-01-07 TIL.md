# 2025.01.06 TIL
### Test 오답 노트
**4. 인터럽트가 발생하면 시스템에서 무엇이 일어나는가?**
- 가) CPU는 현재 작업을 완료하고 인터럽트를 처리한다.
- 나) 현재 작업을 무시하고 바로 인터럽트를 처리한다.
- 다) 인터럽트가 발생하면 시스템은 멈춘다.
- 라) 인터럽트는 사용자가 수동으로 처리해야 한다.

답: 나

풀이: 인터럽트가 발생하면 현재 작업을 무시하고 바로 인터럽트를 처리한다. (머신 코드 level로 생각했음)

**6. 다음 중 입출력 시스템 관리에 포함되지 않는 것은 무엇인가?**
- 가) 버퍼링
- 나) 스풀링
- 다) 디스크 공간 할당
- 라) DMA (Direct Memory Access)

답: 다

풀이: 디스크 공간 할당은 저장 공간 관리를 하는 방법이다.


### Module 2. CS 자료 처리
**알고리즘과 자료구조**
1. 시간복잡도
2. 점근적 표기법
   - 오메가 표기법: 최선의 경우
   - 세타 표기법: 평균적인 경우
   - 빅오 표기법: 최악의 경우
3. 배열
   - 같은 자료형의 데이터를 순서대로 나열한 구조
4. 연결리스트
   - 각 데이터들으 포인터로 연결하여 관리하는 구조
   - 단순 연결 리스트
     - 노드에 포인터 영역이 하나인 연결 리스트
   - 이중 연결 리스트
     - 각 노드에 다음 노드를 가르키는 포인터 영역과 이전 노드를 가리키는 포인터 영역을 포함 
5. 스택
   - 데이터의 삽입과 삭제가 한쪽 방향에서만 일어나는 구조
   - LIFO (= Last-In First-Out)
7. 큐
   - 데이터가 한쪽 방향으로 삽입되고 반대 방향으로 삭제되는 구조
   - FIFO (= First-In First-Out)
8. 그래프
   - 정점과 간선으로 구성된 자료구조
   - 정점(=node / vertex)
   - 간선(=edge)
   - 무방향 그래프
     - 방향성이 없는 간선으로 이루어진 그래프 
   - 방향 그래프
     - 방향성이 있는 간선으로 이루어진 그래프
   - 완전 그래프
     - 모든 정점 사이에 간선이 있는 그래프
   - 가중 그래프
     - 간선마다 가중치를 부여한 그래프
   - 다중 그래프
     - 두정점을 연결하는 다중 간선를 포함하는 그래프
   - 유사 그래프
     - 다중 간선과 루프를 포함하는 그래프
   - 차수
     - 정점에 접한 간선의 수
   - 인접리스트
     - ex) {a: [b, c], b: [a], c: [a]}
   - 인접행렬
     - 그래프에서 정점들 간의 연결 관계를 나타내는 행렬
   - 근접행렬
     - 정점과 간선의 관계를 나타내는 행렬
   - DFS (= 깊이 우선 탐색)
     - 재귀 방식으로 구현
     - 스택을 이용한 반복 방식으로 구현 
   - BFS (= 너비 우선 탐색)
     - 큐를 이용한 구현
9. 트리
   - 서브트리
     - 아래에 있는 노드들이 다시 트리 구조가 되는 구조
   - 부모 노드
     - 임의의 노드 바로 위에 있는 노드
   - 자식 노드
     - 임의의 노드 바로 아래에 있는 노드
   - 형제 노드
     - 같은 부모 노드를 가지는 노드
   - 레벨
     - 루트 노드에서 임의의 노드까지 방문한 노드의 수 (1부터 시작)
   - 깊이
     - 트리의 최대 레벨
   - 이진 트리
     - 모든 노드들의 자식 노드가 두 개 이하인 트리
     - 전위 순회 (루트 -> 왼쪽 -> 오른쪽)
     - 중위 순회 (왼쪽 -> 루트 -> 오른쪽)
     - 후위 순회 (왼쪽 -> 오른쪽 -> 루트)
   - 완전 이진 트리
     - 이진 트리 중에서 단말 노드를 제외한 나머지 노드가 모두 두개의 자식 노드를 가지고 있는 트리
   - 포화 이진 트리
     - 완전 이진 트리 중에서 모든 노드가 채워진 이진 트리
   - 이진 탐색 트리
     - 이진 트리 중에서 같은 데이터를 갖는 노드가 없으며, 왼쪽 서브 트리에 있는 모든 데이터가 현재 노드의 데이터보다 작고, 오른쪽 서브 트리에 있는 모든 데이터가 현재 노드의 데이터보다 큰 트리
   - AVL 트리
     - 균형 이진 탐색 트리
   - 레드-블랙 트리
     - 각 노드에 색깔을 부여하여 트리의 균형을 유지하는 자가 균형 이진 탐색 트리
   - 힙
     - 최소 힙: 부모 노드 <= 자식 노드
     - 최대 힙: 부모 노드 >= 자식 노드
     - 활용) 우선순위 큐, 힙 정렬, 최소 신장 트리
10. 정렬
    - 선택 정렬
      - 정렬되지 않은 데이터 중에서 가장 작은 데이터를 찾아 가장 앞의 데이터와 교환 해나가는 방식
    - 삽입 정렬
      - 아직 정렬되지 않은 임의의 데이터를 이미 정렬된 부분의 적절한 위치에 삽입해가며 정렬하는 방식
    - 버블 정렬
      - 서로 이웃한 데이터들을 비교해 가장 큰 데이터를 맨 뒤로 보내며 정렬하는 방식
    - 퀵 정렬
      - 기준키를 중심으로 작거나 같은 값을 지닌 데이터는 앞으로, 큰 값을 지닌 데이터는 뒤로 가도록 하여 작은 값을 갖는 데이터와 큰 값을 갖는 데이터로 분리해가며 정렬하는 방법
    - 병합 정렬
      - 분할 정복 방식으로 작동하는 정렬 방법
    - 힙 정렬
      - 힙 자료구조를 사용하여 정렬을 수행하는 방법
11. 탐색
    - 선형 탐색
      - 주어진 데이터 집합에서 원하는 데이터를 처음부터 순차적으로 비교하며 찾는 방법
    - 이진 탐색
      - 정렬된 데이터 집합을 반으로 쪼개 가면서 탐색하는 방법
12. 해시 테이블
    - 키를 사용하여 데이터를 빠르게 검색할 수 있도록 설계된 자료구조
    - 해시 충돌
      - 해시 함수가 서로 다른 키들에 대해 동일한 해시값을 반환하는 경우
    - 체이닝
      - 같은 인덱스에 여러 값을 저장할 수 있도록 연결 리스트를 사용하여 충돌을 해결
    - 개방 주소법
      - 충돌이 방생하면 다른 빈 슬롯을 찾아 데이터를 삽입하는 방식
      - ex) 선형 탐사, 이차 탐사, 이중 해싱 등 
14. 재귀 알고리즘
    - 자기 자신을 호출하는 방식으로 문제를 해결하는 알고리즘
    - ex) 팩토리얼 계산, 피보나치 수열
16. 그리디 알고리즘
    - 매 순간 최적의 선택을 하는 방법으로 문제를 해결하는 알고리즘
18. 최소 신장 트리
    - 주어진 그래프에서 모든 노드를 연결할 수 있는 트리 구조를 찾고, 그 트리의 간선들의 가중치 합이 최소가 되도록 만드는 문제
    - 크루스칼 알고리즘
      - 그래프의 간선들을 가중치가 작은 순서대로 정렬하고 그 간선들을 하나씩 선택하여 최소 신장 트리를 만듬. 간선을 선택할 때 싸이클이 발생하지 않도록 확인하며 반복
    - 프림 알고리즘
      - 임의의 정점에서 시작하여 그 정점과 연결된 간선 중 가중치가 가장 작은 간선을 선택하고, 선택된 간선에 연결된 정점을 확장해 가면서 최소 신장 트리를 만듬
19. 다익스트라 알고리즘
    - 단일 출발지 최단 경로 문제를 해결하는 알고리즘
    - 주어진 그래프에서 한 노드에서 다른 노드까지의 최단 경로를 찾는 데 사용
    - 가중치가 있는 그래프에서 동작하며, 그래프의 모든 노드에 대해 최단 경로를 계산
    - 가중치가 음수인 경우에는 적용할 수 없으며, 그런 경우 벨만-포드 알고리즘을 사용해야 함
20. 동적 프로그래밍(=DP)
    - 복잡한 문제를 작은 부분 문제로 나누어 해결하고, 그 결과를 저장하여 중복 계산을 방지하는 알고리즘 설계 기법
    - 주로 최적화 문제에서 사용되며, 각 부분 문제를 해결하고 그 해결 결과를 테이블에 저장하여 이후의 계산에서 재사용
    - 중복되는 부분 문제가 존재할 때 유용하게 사용
    - 메모이제이션
      - 재귀를 사용하여 문제를 풀고, 중간 계산 결과를 저장하여 중복 계산을 방지
      - 문제를 해결하는 과정에서 한 번 계산된 값은 저장하고, 다시 계산하지 않도록 함
    - 타뷸레이션
      - 재귀를 사용하지 않고 반복문을 통해 해결. 작은 부분 문제부터 차례대로 풀어 최종적으로 전체 문제를 해결
      - DP 테이블을 채워가며 계산하는 방식
