# 학습 날짜 : 2025-02-05 TIL
## 객체 지향

### Access Modifier

- **public** : 어디에서나 접근 가능 / class, field, method, constructor 선언에 사용 가능
- **default** : 같은 package 내에서 접근 가능 / class, field, method, constructor 선언에 사용 가능
- **private** : 다른 class에서 접근 불가능 / field, method, constructor 선언에 사용 가능
- **protected** : 같은 package 내 또는 상속 관계에서 접근 가능 / field, method, constructor 선언에 사용 가능

### Static Block

```java
package lecture;

class StaticBlock {

    int aaa;
    static int bbb = staticCall();

    public static int staticCall() {
        System.out.println("static method call()");
        return 100;
    }

    // method area 위치에 다 올린 후, static block 실행
    // 일반적으로 비지니스 로직 처리 X
    static {
        System.out.println("이것은 Static block의 연습입니다. bbb의 값은 : " + bbb);
    }

    public static void main(String[] args) {
        System.out.println("main() 호출");
    }
}
```

### 상속 (inheritance)

- 부모 클래스 (Parent Class, Super Class, 상위 클래스 등)
- 자식 클래스 (Child Class, Sub Class, 하위 클래스 등)
- JAVA는 단일 상속만 제공
- tree 구조로 만들어지게 된다
- 자바의 모든 class는 상속 관계에 있다 (항상 java.lang.Object를 상속)
