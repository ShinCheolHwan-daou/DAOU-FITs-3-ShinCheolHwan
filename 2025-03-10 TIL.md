## 어떤 문제가 있었는지
```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main(void)
{
  char s[] = "swimming";
  char t[5] = "야구";
  printf("s:%s\n", s);
  printf("s[2]:%c\n", s[2]);
  printf("t:%s\n", t);
  printf("t:%c%c\n", t[2], t[3]); // error!
  return 0;
}
```
다음과 같은 문자열 관련 코드를 따라하는데 마지막 printf에서 "구"가 나와야 했지만 문자가 깨져서 나왔습니다.

## 그 문제를 해결하기 위해 내가 시도해본 것들
코드 인코딩 방식이나 컴파일러 인코딩 방식에 문제가 있는 것 같아서 해당 내용을 찾아봤습니다.

## 어떻게 그 문제를 해결했는지
현재 코드가 짜여진 버전에서 인코딩 방식은 한글을 2byte로 처리하는 EUC-KR 기반이였고, 제 컴퓨터는 한글을 3byte로 처리하는 UTF-8 기반이였습니다.<br>
따라서 3byte로 처리하기 위해 코드를 다음과 같이 바꿨고 해결할 수 있었습니다.
```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main(void)
{
  char s[] = "swimming";
  char t[5] = "야구";
  printf("s:%s\n", s);
  printf("s[2]:%c\n", s[2]);
  printf("t:%s\n", t);
  printf("t:%c%c%c\n", t[3], t[4], t[5]); // error!
  return 0;
}
```

## 오늘 내가 새롭게 배운 것들
인코딩 방식에 따라 한글 같은 경우, 바이트 수가 다를 수 있다는 내용을 알게 되었습니다.
