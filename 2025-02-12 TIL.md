# 학습 날짜 : 2025년 02월 12일

## JDBC MVC Pattern

### Layered Architecture

- 4 tier
- 인접한 Layer끼리만 소통 가능
- Presentation Layer (입출력)
- Business Layer (로직)
- Persistence Layer (Database 처리)
- Database Layer (Oracle, MySQL)

### MVC Pattern

- Model
    - Data Model: VO
    - Service Model: 로직 처리 전담 객체
        - 순수 로직 처리
        - DB 처리 ⇒ DAO로 분리
- View
    - 입출력 담당 (presentation layer)
- Controller
    - View와 Model을 연결해주는 역할

** VO (= Value Object): business 로직 X, but 무결성 처리는 가능

### Template Method Pattern

1. 일반적인 DAO 
    
    → 중복 코드 존재 (ex: Database 연결)
    
2. 중복된 코드를 method로 분리 (추출) : class의 유지/보수성 상승 
    
    → 환경에 따른 변수 존재
    
3. 추상 method와 추상 class로 만들어서 제공: 가져가서 사용하는 사람이 상속해서 사용
    - 상위 class에서 기본적인 logic을 구현
    - 하위 class에서 구체적인 logic을 변경해서 구현
    
    → **단일 상속 문제** 때문에 사용이 불가능한 경우도 존재
    
4. UserDAO의 변경해야 하는 부분을 별도의 class로 분리 (SimpleConnectionMaker class 생성) 
    
    → UserDAO를 재사용하기 위해 반드시 UserDAO에 명시적으로 박혀있는 class를 만들어서 사용해야 한다 (**tightly coupled**)
    
5. interface와 UserDAO를 제공 : Dependency Injection을 통해 ConnectionMaker 제공
