# 2025.01.15 TIL
### Test 오답 노트
**1. 데이터베이스에서 중복된 데이터를 제거하는 키워드는?**

답: DISTINCT

풀이
```SQL
-- city 테이블에서 region컬럼에 대해 중복 제거하여 출력
SELECT DISTINCT region FROM city;
```

**5. ERD 작성 시 다음 중 엔터티의 주요 구성 요소가 아닌 것은?**

1. 속성(Attribute)
2. 관계(Relationship)
3. 테이블(Table)
4. 기본 키(Primary Key)

답: 3

풀이: 테이블은 RDB의 구성 요소이다.

**9. 정규화를 거치면 발생할 수 있는 문제는?**

1. 데이터 검색 속도 저하
2. 데이터 중복 증가
3. 트랜잭션 오류 발생
4. 데이터 무결성 문제

답: 1

풀이: 문제를 반정규화로 읽었다.

----
### Module 3 SQL 기초
1. ORDER BY
   - SQL에서 데이터를 정렬하는데 사용되는 절
   - 기본적으로 오름차순 정렬
   - 내림차순 정렬시 DESC 키워드 사용
   - 하나 이상의 컬럼을 기준으로 정렬 가능
   - ORACLE에서 NULL 값은 최대값으로 간주
   - NULL FIRST OR NULL LAST 사용 가능
```MYSQL
SELECT NAME FROM CUSTOMERS ORDER BY NAME ASC;
SELECT ACCOUNT_ID, BALANCE FROM ACCOUNTS ORDER BY BALANCE DESC;
SELECT TRANSACTION_ID, AMOUNT FROM TRANSACTIONS ORDER BY AMOUNT ASC;
SELECT LOAN_ID, CUSTOMER_ID FROM LOANS ORDER BY AMOUNT DESC;

SELECT NAME, EMAIL FROM CUSTOMERS ORDER BY NAME ASC, EMAIL DESC;
SELECT * FROM ACCOUNTS ORDER BY BALANCE DESC, ACCOUNT_ID ASC;
SELECT * FROM TRANSACTIONS ORDER BY TRANSACTION_TYPE ASC, AMOUNT DESC;
SELECT * FROM LOANS ORDER BY STATUS ASC, AMOUNT DESC;
```
2. 페이징 쿼리
   - 대량의 데이터에서 일정한 크기로 데이터를 나누어 보여주는 방법
```MYSQL
SELECT * FROM (
    SELECT 
        ROW_NUMBER() OVER (ORDER BY CREATED_AT DESC) AS ROW_NUM, 
        CUSTOMER_ID, 
        CREATED_AT 
        FROM CUSTOMERS
) WHERE ROW_NUM BETWEEN :START_ROW AND :END_ROW;

SELECT * FROM (SELECT S.*, ROWNUM AS ROW_NUM FROM (
    SELECT * FROM EMPLOYEES WHERE SALARY IS NOT NULL ORDER BY SALARY DESC
) S) WHERE ROW_NUM BETWEEN 11 AND 20;

SELECT *
FROM EMPLOYEES
ORDER BY SALARY
OFFSET :START_ROW - 1 ROWS FETCH NEXT:PAGE_SIZE ROWS ONLY;

SELECT E.*, ROW_NUMBER() OVER (ORDER BY SALARY DESC) + :START_ROW - 1 AS IDX
FROM EMPLOYEES E
WHERE SALARY IS NOT NULL
ORDER BY SALARY DESC
OFFSET :START_ROW - 1 ROWS FETCH NEXT :PAGE_SIZE ROWS ONLY;
```
3. DISTINCT
   - SQL에서 중복된 데이터를 제거하여 고유한 값만 반환하는 키워드
   - DISTINCT 다음에 여러 개의 컬럼이 나올 경우, 모두 중복의 기준에 적용됨
```MYSQL
SELECT DISTINCT ACCOUNT_TYPE FROM ACCOUNTS;
SELECT DISTINCT TRANSACTION_TYPE FROM TRANSACTIONS;
SELECT DISTINCT CUSTOMER_ID, ACCOUNT_TYPE FROM ACCOUNTS;
```
4. GROUP BY
   - HAVING 절은 GROUP BY 이후 그룹별 조건 필터링
```MYSQL
SELECT 
    STATUS,
    SUM(AMOUNT) AS TOTAL_AMOUNT, 
    COUNT(*) AS TOTAL_COUNT
FROM LOANS WHERE STATUS = 'APPROVED' GROUP BY STATUS;

SELECT 
    TO_CHAR(TRANSACTION_DATE, 'YYYY-MM') AS MONTH, 
    SUM(AMOUNT) AS MONTHLY_AMOUNT
FROM TRANSACTIONS
GROUP BY TO_CHAR(TRANSACTION_DATE, 'YYYY-MM')
HAVING SUM(AMOUNT) >= 10000;

SELECT 
    BRANCH_ID,
    AVG(AMOUNT) AS AVG_AMOUNT,
    SUM(AMOUNT) AS TOTAL_AMOUNT
FROM LOANS
WHERE STATUS = 'APPROVED'
GROUP BY BRANCH_ID;
```
5. 집계 함수
   - COUNT(), SUM(), AVG(), MAX(), MIN()
6. 문자 함수
   - UPPER(), LOWER(), LENGTH(), SUBSTR(), INSTR(), TRIM(), LPAD(), RPAD(), REPLACE(), CONCAT()
7. 숫자 함수
   - ROUND(), TRUNC(), ABS(), MOD(), CEIL(), FLOOR()
8. 날짜 함수
   - SYSDATE, CURRENT_DATE, ADD_MONTHS(), MONTHS_BETWEEN(), NEXT_DAY(), LAST_DAY(), EXTRACT(), TRUNC()
9. 변환 함수
    - TO_CHAR(), TO_DATE(), TO_NUMBER()
10. NULL 처리 함수
    - NVL(), NVL2(), COALESCE(), NULLIF()
11. CASE WHEN
```MYSQL
SELECT
    EMPLOYEE_ID,
    CASE
        WHEN SALARY > 10000 THEN 'HIGH'
        WHEN SALARY BETWEEN 5000 AND 10000 THEN 'MEDIUM'
        ELSE 'LOW'
    END AS SALARY_LEVEL
FROM EMPLOYEES;
```
13. DECODE
```MYSQL
SELECT
    EMPLOYEE_ID,
    DECODE(JOB_ID, 'ADMIN', '관리자', 'DEV', '개발자', 'HR', '인사담당자', '기타') AS DEPARTMENT_NAME
FROM EMPLOYEES;
```

----
### Module 4 SQL 중급
1. JOIN
   - 여러 테이블의 데이터를 결합하여 하나의 결과 집합을 생성하는 SQL 절
   - 기본적으로 두 개 이상의 테이블을 연결
   - 테이블 간 관계를 활용하여 데이터 조회
2. INNER JOIN
   - 두 테이블 간에 일치하는 데이터만 반환
   - 기본적인 JOIN 방식
4. OUTER JOIN
   - LEFT OUTER JOIN : LEFT 테이블의 모든행과 일치하는 RIGHT 테이블의 데이터를 반환
   - RIGHT OUTER JOIN : RIGHT 테이블의 모든 행과 일치하는 LEFT 테이블의 데이터를 반환
   - FULL OUTER JOIN : LEFT 테이블과 RIGHT 테이블의 모든 행 반환
   - 일치하지 않는 데이터는 NULL로 표시
6. SELF JOIN
   - 같은 테이블을 두 번 사용하여 JOIN 수행
   - 주로 계층 구조를 표현할 떄 사용
8. CROSS JOIN
   - 두 테이블 간의 모든 행의 조합을 반환 (Cartensian Product)
   - JOIN 조건이 없는 경우 발생
   - 결과 집합의 크기가 매우 커질 수 있으므로 조심해서 사용
10. NATURAL JOIN
    - 동일한 이름의 컬럼을 기준으로 JOIN
    - 조건을 명시하지 않아도 자동으로 연결
