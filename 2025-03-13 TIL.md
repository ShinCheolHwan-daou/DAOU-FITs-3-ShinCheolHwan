## 어떤 문제가 있었는지
추가문제를 푸는 과정에서 이중 배열을 이중 포인터의 함수 argument로 넘기는 과정에서 에러가 났습니다.

## 그 문제를 해결하기 위해 내가 시도해본 것들
int **를 사용해서 초기에는 int arr[3][3]을 지정해 바로 넘기려고 했습니다.

## 어떻게 그 문제를 해결했는지
arr의 형식은 int arr이기 때문에 배열 안에는 포인터가 아닌 int가 담겨있었습니다. 따라서 int *dp[3]을 새롭게 지정하여 array[i]의 주소 값을 넣어주었습니다.<br>
다음은 코드 중 일부 입니다.
```C
void doublePointer4() {
    int select;
    void (*f[])(int **, int, int) = {sum, max, min, square};
    int array[ROWS][COLS] = {
        {0, 1, 2},
        {3, 4, 5},
        {6, 7, 8}
    };
    int *dp[ROWS];
    for (int i = 0; i < ROWS; i++) {
        dp[i] = array[i];
    }

    printf("연산 방법을 선택하기");
    printf("(0: 합, 1: 최대값, 2: 최소값, 3: 제곱): ");
    scanf("%d", &select);

    if (select < 0 || select > 3) {
        return;
    }

    f[select](dp, ROWS, COLS);
}
```

## 오늘 내가 새롭게 배운 것들
포인터를 사용해서 argument를 지정할 때 사용하는 규칙에 대해 새롭게 알게 되었습니다.
- 2D 배열을 함수에 넘길 때 int (*matrix)[COLS] 방식이 가장 안정적이고 일반적임.
- int **matrix는 동적 할당된 2D 배열에만 적합하며, 일반적인 배열에는 적절하지 않음.
- C에서는 배열의 행(row) 크기는 함수에서 생략할 수 있지만, 열(column) 크기는 반드시 필요함.
