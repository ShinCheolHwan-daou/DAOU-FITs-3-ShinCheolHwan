# 학습 날짜: 2025년 02월 17일

## Servlet 기본

### Servlet

1. Web Porject 생성
    - JDK 버전, tomcat 버전, Jakarta EE 버전
2. Tomcat을 이용해서 deploy
    - application context를 수정해서 사용 가능
3. Servlet 사용 방법
    - Servlet : interface
    - Servlet - GenericServlet class - HttpServlet class
    - 주요 method
        - init() : Servlet 객체가 생성될 때 호출되는 초기화를 담당하는 메소드 (생성자를 대신함)
        - service() : 클라이언트가 servlet을 실행할 때 호출되는 method / 이미 구현되어 있음
        - doGet(HttpServletRequest request, HttpServletResponse response)
        - doPost(HttpServletRequest request, HttpServletResponse response)
        - destroy() : Servlet instance가 WAS에서 삭제될 때 수행되는 method
            
            ⇒ 사용하고 있는 여러 resource들을 해제
            
4. 클라이언트가 GET 방식으로 Servlet을 호출했을 때 간단하게 response를 전송하는 Servlet을 실제로 구현
    - 클라이언트가 Servlet을 호출하는 방법을 제공해야 함
    - Servlet에 논리적인 이름을 부여해서 클라이언트가 이용해서 Servlet을 호출 (URL mapping)
        1. xml 파일 이용
        2. annotation 사용

### 데이터 전송 방식

1. GET 방식
    - QueryString 이용해서 데이터를 전달하는 방식
    - QueryString은 특수한 형식을 가진 문자열
    - ?변수1=값1&변수2=값2 …
    - getParameter(”변수명”)
    - 보안성 문제
    - 길이 제한 문제

1. POST 방식
    - 보안성이 약간 있음 (브라우저 주소창에 변수와 값에 대해 표현되지 않음)
    - request header에 포함되어 데이터 전달 → 길이의 제한이 없다
    - HTML의 특수한 태그를 이용해서 서버를 호출하는 구조 (Form tag)
